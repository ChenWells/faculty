# 房東管理系統實作計劃

## 1. 專案初始化

### 1.1 開發環境設定

#### 必要工具安裝
```bash
# Node.js (建議版本 18.x 或以上)
# 下載並安裝 Node.js: https://nodejs.org/

# Git
# 下載並安裝 Git: https://git-scm.com/

# 程式碼編輯器 (推薦 VS Code)
# 下載並安裝 VS Code: https://code.visualstudio.com/

# 資料庫 (PostgreSQL)
# 下載並安裝 PostgreSQL: https://www.postgresql.org/
```

#### 驗證安裝
```bash
# 檢查 Node.js 版本
node --version
npm --version

# 檢查 Git 版本
git --version

# 檢查 PostgreSQL 版本
psql --version
```

### 1.2 專案結構建立

```bash
# 建立專案目錄
mkdir landlord-management-system
cd landlord-management-system

# 初始化 Git 倉庫
git init

# 建立專案結構
mkdir -p frontend backend docs database
```

### 1.3 專案結構
```
landlord-management-system/
├── frontend/                 # React/Next.js 前端
├── backend/                  # Node.js/Express 後端
├── database/                 # 資料庫相關檔案
├── docs/                     # 專案文件
├── .gitignore               # Git 忽略檔案
├── README.md                # 專案說明
└── package.json             # 根目錄 package.json
```

## 2. 後端開發 (第一階段)

### 2.1 後端專案初始化

```bash
cd backend

# 初始化 Node.js 專案
npm init -y

# 安裝核心依賴
npm install express cors helmet morgan dotenv
npm install jsonwebtoken bcryptjs
npm install google-auth-library googleapis
npm install pg sequelize
npm install nodemailer
npm install cron
npm install joi
npm install winston

# 安裝開發依賴
npm install --save-dev nodemon eslint prettier
npm install --save-dev jest supertest
```

### 2.2 後端目錄結構

```
backend/
├── src/
│   ├── config/              # 設定檔案
│   │   ├── database.js
│   │   ├── google.js
│   │   └── app.js
│   ├── models/              # 資料模型
│   │   ├── User.js
│   │   ├── Property.js
│   │   ├── Tenant.js
│   │   ├── Contract.js
│   │   ├── ElectricityRecord.js
│   │   └── PaymentRecord.js
│   ├── controllers/         # 控制器
│   │   ├── authController.js
│   │   ├── propertyController.js
│   │   ├── tenantController.js
│   │   ├── contractController.js
│   │   ├── electricityController.js
│   │   └── paymentController.js
│   ├── services/            # 服務層
│   │   ├── googleAuthService.js
│   │   ├── googleSheetsService.js
│   │   ├── lineNotificationService.js
│   │   └── calculationService.js
│   ├── middleware/          # 中間件
│   │   ├── auth.js
│   │   ├── validation.js
│   │   └── errorHandler.js
│   ├── routes/              # 路由
│   │   ├── auth.js
│   │   ├── properties.js
│   │   ├── tenants.js
│   │   ├── contracts.js
│   │   ├── electricity.js
│   │   └── payments.js
│   ├── utils/               # 工具函數
│   │   ├── logger.js
│   │   ├── validation.js
│   │   └── helpers.js
│   └── app.js               # 主應用程式
├── tests/                   # 測試檔案
├── migrations/              # 資料庫遷移
├── seeds/                   # 資料庫種子資料
├── .env.example            # 環境變數範例
├── .gitignore
└── package.json
```

### 2.3 基礎設定檔案

#### 環境變數設定 (.env)
```bash
# 資料庫設定
DB_HOST=localhost
DB_PORT=5432
DB_NAME=landlord_management
DB_USER=postgres
DB_PASSWORD=your_password

# Google OAuth 設定
GOOGLE_CLIENT_ID=your_google_client_id
GOOGLE_CLIENT_SECRET=your_google_client_secret
GOOGLE_REDIRECT_URI=http://localhost:3000/auth/google/callback

# JWT 設定
JWT_SECRET=your_jwt_secret_key
JWT_EXPIRES_IN=7d

# LINE API 設定
LINE_CHANNEL_ACCESS_TOKEN=your_line_channel_access_token
LINE_CHANNEL_SECRET=your_line_channel_secret

# 伺服器設定
PORT=3000
NODE_ENV=development
```

#### 主應用程式 (app.js)
```javascript
const express = require('express');
const cors = require('cors');
const helmet = require('helmet');
const morgan = require('morgan');
require('dotenv').config();

const app = express();

// 中間件
app.use(helmet());
app.use(cors());
app.use(morgan('combined'));
app.use(express.json());
app.use(express.urlencoded({ extended: true }));

// 路由
app.use('/api/auth', require('./src/routes/auth'));
app.use('/api/properties', require('./src/routes/properties'));
app.use('/api/tenants', require('./src/routes/tenants'));
app.use('/api/contracts', require('./src/routes/contracts'));
app.use('/api/electricity', require('./src/routes/electricity'));
app.use('/api/payments', require('./src/routes/payments'));

// 錯誤處理
app.use(require('./src/middleware/errorHandler'));

const PORT = process.env.PORT || 3000;
app.listen(PORT, () => {
  console.log(`Server running on port ${PORT}`);
});
```

### 2.4 資料庫設定

#### 資料庫連接 (config/database.js)
```javascript
const { Sequelize } = require('sequelize');

const sequelize = new Sequelize(
  process.env.DB_NAME,
  process.env.DB_USER,
  process.env.DB_PASSWORD,
  {
    host: process.env.DB_HOST,
    port: process.env.DB_PORT,
    dialect: 'postgres',
    logging: process.env.NODE_ENV === 'development' ? console.log : false,
    pool: {
      max: 5,
      min: 0,
      acquire: 30000,
      idle: 10000
    }
  }
);

module.exports = sequelize;
```

### 2.5 核心模型建立

#### 用戶模型 (models/User.js)
```javascript
const { DataTypes } = require('sequelize');
const sequelize = require('../config/database');

const User = sequelize.define('User', {
  id: {
    type: DataTypes.UUID,
    defaultValue: DataTypes.UUIDV4,
    primaryKey: true
  },
  googleId: {
    type: DataTypes.STRING,
    unique: true,
    allowNull: false
  },
  email: {
    type: DataTypes.STRING,
    unique: true,
    allowNull: false
  },
  name: {
    type: DataTypes.STRING,
    allowNull: false
  },
  picture: {
    type: DataTypes.STRING
  },
  role: {
    type: DataTypes.ENUM('admin', 'manager', 'tenant'),
    defaultValue: 'manager'
  },
  accessToken: {
    type: DataTypes.TEXT
  },
  refreshToken: {
    type: DataTypes.TEXT
  }
});

module.exports = User;
```

## 3. 前端開發 (第二階段)

### 3.1 前端專案初始化

```bash
cd frontend

# 使用 Next.js 建立專案
npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"

# 安裝額外依賴
npm install @react-oauth/google
npm install axios
npm install react-hook-form
npm install @hookform/resolvers zod
npm install react-query
npm install date-fns
npm install recharts
npm install @headlessui/react
npm install @heroicons/react
```

### 3.2 前端目錄結構

```
frontend/
├── src/
│   ├── app/                 # Next.js 13+ App Router
│   │   ├── layout.tsx
│   │   ├── page.tsx
│   │   ├── login/
│   │   ├── dashboard/
│   │   ├── properties/
│   │   ├── tenants/
│   │   ├── contracts/
│   │   ├── electricity/
│   │   └── payments/
│   ├── components/          # 可重用組件
│   │   ├── common/
│   │   ├── forms/
│   │   ├── layout/
│   │   └── ui/
│   ├── hooks/               # 自定義 Hooks
│   ├── services/            # API 服務
│   ├── utils/               # 工具函數
│   ├── types/               # TypeScript 類型定義
│   └── styles/              # 樣式檔案
├── public/                  # 靜態檔案
├── .env.local              # 環境變數
└── package.json
```

### 3.3 核心組件建立

#### 登入頁面 (app/login/page.tsx)
```typescript
'use client';

import { GoogleLogin } from '@react-oauth/google';
import { useRouter } from 'next/navigation';
import { useState } from 'react';

export default function LoginPage() {
  const router = useRouter();
  const [isLoading, setIsLoading] = useState(false);

  const handleGoogleSuccess = async (credentialResponse: any) => {
    setIsLoading(true);
    try {
      const response = await fetch('/api/auth/google/verify', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
        },
        body: JSON.stringify({
          credential: credentialResponse.credential,
        }),
      });

      const data = await response.json();

      if (data.token) {
        localStorage.setItem('token', data.token);
        router.push('/dashboard');
      }
    } catch (error) {
      console.error('Login error:', error);
    } finally {
      setIsLoading(false);
    }
  };

  return (
    <div className="min-h-screen flex items-center justify-center bg-gray-50">
      <div className="max-w-md w-full space-y-8">
        <div>
          <h2 className="mt-6 text-center text-3xl font-extrabold text-gray-900">
            房東管理系統
          </h2>
          <p className="mt-2 text-center text-sm text-gray-600">
            使用Google帳號登入
          </p>
        </div>
        <div className="mt-8 space-y-6">
          <GoogleLogin
            onSuccess={handleGoogleSuccess}
            onError={() => console.log('Login Failed')}
            scope="https://www.googleapis.com/auth/userinfo.email https://www.googleapis.com/auth/userinfo.profile https://www.googleapis.com/auth/spreadsheets https://www.googleapis.com/auth/drive.file"
          />
          {isLoading && (
            <div className="text-center">
              <div className="animate-spin rounded-full h-8 w-8 border-b-2 border-blue-600 mx-auto"></div>
            </div>
          )}
        </div>
      </div>
    </div>
  );
}
```

#### 主佈局 (app/layout.tsx)
```typescript
import { GoogleOAuthProvider } from '@react-oauth/google';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="zh-TW">
      <body className={inter.className}>
        <GoogleOAuthProvider clientId={process.env.NEXT_PUBLIC_GOOGLE_CLIENT_ID!}>
          {children}
        </GoogleOAuthProvider>
      </body>
    </html>
  );
}
```

## 4. 開發時程安排

### 4.1 第一週：基礎架構
- [ ] 後端專案初始化
- [ ] 資料庫設定和模型建立
- [ ] Google OAuth 整合
- [ ] 基本 API 路由設定

### 4.2 第二週：核心功能
- [ ] 用戶認證系統
- [ ] 房屋管理功能
- [ ] 租客管理功能
- [ ] 前端基礎架構

### 4.3 第三週：業務邏輯
- [ ] 合約管理系統
- [ ] 電費計算功能
- [ ] 費用計算邏輯
- [ ] 前端表單和列表

### 4.4 第四週：通知和同步
- [ ] LINE 通知整合
- [ ] Google Sheets 同步
- [ ] 收租管理功能
- [ ] 前端儀表板

### 4.5 第五週：優化和測試
- [ ] 系統測試
- [ ] 效能優化
- [ ] 錯誤處理
- [ ] 文件撰寫

## 5. 開發工具和流程

### 5.1 版本控制
```bash
# 建立分支策略
git checkout -b develop
git checkout -b feature/user-authentication
git checkout -b feature/property-management
git checkout -b feature/contract-management
```

### 5.2 開發流程
1. **功能分支開發**
   - 從 develop 分支建立功能分支
   - 開發完成後提交 Pull Request
   - 程式碼審查後合併到 develop

2. **測試流程**
   - 單元測試
   - 整合測試
   - 端到端測試

3. **部署流程**
   - 開發環境：自動部署
   - 測試環境：手動部署
   - 生產環境：手動部署

### 5.3 程式碼品質
```bash
# ESLint 設定
npm run lint

# Prettier 格式化
npm run format

# 測試執行
npm run test

# 建置檢查
npm run build
```

## 6. 部署準備

### 6.1 環境準備
- **開發環境**：本地開發
- **測試環境**：Vercel (前端) + Railway (後端)
- **生產環境**：Vercel (前端) + Railway (後端)

### 6.2 環境變數設定
```bash
# 生產環境變數
NEXT_PUBLIC_API_URL=https://your-api-domain.com
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your_production_client_id
```

### 6.3 監控和日誌
- 使用 Winston 進行日誌記錄
- 設定錯誤監控 (Sentry)
- 設定效能監控

## 7. 開始實作的具體步驟

### 步驟1：環境準備
```bash
# 1. 安裝必要工具
# 2. 建立專案目錄
mkdir landlord-management-system
cd landlord-management-system

# 3. 初始化 Git
git init
git remote add origin your-repository-url
```

### 步驟2：後端開發
```bash
# 1. 建立後端目錄
mkdir backend
cd backend

# 2. 初始化 Node.js 專案
npm init -y

# 3. 安裝依賴
npm install express cors helmet morgan dotenv
# ... (其他依賴)

# 4. 建立基礎檔案結構
mkdir -p src/{config,models,controllers,services,middleware,routes,utils}
```

### 步驟3：前端開發
```bash
# 1. 建立前端專案
cd ..
npx create-next-app@latest frontend --typescript --tailwind --eslint --app --src-dir --import-alias "@/*"

# 2. 安裝額外依賴
cd frontend
npm install @react-oauth/google axios react-hook-form
```

### 步驟4：Google Cloud 設定
1. 前往 Google Cloud Console
2. 建立新專案
3. 啟用必要 API
4. 建立 OAuth 2.0 憑證
5. 下載服務帳號金鑰

### 步驟5：資料庫設定
```bash
# 1. 安裝 PostgreSQL
# 2. 建立資料庫
createdb landlord_management

# 3. 設定環境變數
cp .env.example .env
# 編輯 .env 檔案
```

## 8. 下一步行動

1. **立即開始**：按照上述步驟建立專案基礎架構
2. **設定 Google Cloud**：完成 OAuth 和 API 設定
3. **建立資料庫**：設定 PostgreSQL 和基本模型
4. **開發認證系統**：實現 Google OAuth 登入
5. **逐步實現功能**：按照時程安排開發各模組

這個實作計劃提供了完整的開發路徑，從環境設定到部署上線。建議您按照這個計劃逐步實施，每個階段都要確保功能穩定後再進行下一步。 